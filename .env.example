# Google OAuth identifiers are public configuration, not secrets.
# The deployed values live in public/auth-config.json.
# Passwords, OAuth client secrets and identity tokens must never be committed.

# Optional private JSON endpoint created from the Google Sheet Website_Export tab.
# Keep the deployed website private before enabling real business data.
NEXT_PUBLIC_DASHBOARD_DATA_URL=https://your-private-endpoint.example.com/dashboard
